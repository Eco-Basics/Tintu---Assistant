---
phase: 03-context-budget-manager
task: complete
total_tasks: 5
status: milestone_complete_vps_pending
last_updated: 2026-03-29T00:42:26.329Z
---

<current_state>
Milestone v1.0 is fully executed. All 3 phases, 11 plans done. 33/33 tests
passing. Bot runs on Windows dev machine — confirmed startup, task/note
handling work. Two bugs found and fixed during live testing (tzdata, aiosqlite
double-thread). Five live-Ollama verification items deferred to VPS.

Next session = connect VPS + run deferred checks.
</current_state>

<completed_work>

**Phase 1 — Foundation** (2/2 plans)
- 01-01: pytest scaffold + CAPABILITY_REFUSALS guard in router.py — 5 tests ✓
- 01-02: personality_traits + personas schema migration — 4 tests ✓

**Phase 2 — PromptBuilder** (4/4 plans)
- 02-01: Phase 2 test harness + LOG_LEVEL fix ✓
- 02-02: build_system_prompt() TDD in app/llm/prompt_builder.py — 6 tests ✓
- 02-03: system param on compare_against_prior() ✓
- 02-04: Dynamic prompt wired into all 4 call sites + PERS-04 preference echo ✓

**Phase 3 — Context Budget Manager** (5/5 plans)
- 03-01: conversation_turns table + key_facts/named_entities columns + 15 test stubs ✓
- 03-02: ConversationCache singleton (16-msg cap) + turn write + startup reload ✓
- 03-03: ContextBudgetManager (8192 token cap, oldest-first trim) + 4 call sites ✓
- 03-04: 20-turn summarizer + key_facts extraction + /summarize + correction flow ✓
- 03-05: Session continuity signal (seamless/resume/fresh) ✓

**Bugs fixed during live testing**
- fix(time): datetime.timezone.utc for UTC — no tzdata needed on Windows
- fix(db): @asynccontextmanager on get_db() — prevents double thread-start
- fix(main): guard job_queue is not None before setup_jobs()

**Test suite: 33/33 passing**
</completed_work>

<remaining_work>

**Deferred to VPS (all in 03-VERIFICATION.md → "Human Verification Deferred")**

1. PERS-02 — History recall accuracy
   - Send 5-6 messages with specific facts, ask "what do I prefer?"
   - Expected: assistant references earlier message accurately

2. CTX-01 — Token budget alignment
   - Check DEBUG logs for "Context assembled chat_id=... tokens_used=X/8192"
   - Expected: count_tokens(prompt) ≈ prompt_eval_count from Ollama (±5-10%)

3. CTX-02 — Task injection visibility
   - Add 3-5 tasks, ask a general question
   - Expected: response acknowledges or references active work

4. CTX-03 — Continuity signal on restart
   - Shut down + restart bot, send first message
   - Expected: "Resuming from last session summary." OR "No prior session found — starting fresh." prefixed once only

5. PERS-03 — Auto-summarize at 20 turns
   - Send exactly 20 messages
   - Expected: separate Telegram summary message after turn 20; user can reply with correction

**Also needed on VPS**
- pip install "python-telegram-bot[job-queue]" to enable scheduled reminders
</remaining_work>

<decisions_made>

- No behavior_preferences table — existing preferences table fulfils that role (Phase 2 reads from it directly)
- Monkeypatch target is always app.bot.router.X (imported binding), not source module — applies across all tests
- ContextBudgetManager wraps system prompt + history + tasks — single assembly point; Phase 2's build_system_prompt() became one section within the budget
- History cap: 16 messages in-memory, full history in DB — summaries capture what cache drops
- Trim oldest history pairs first, never trim system prompt or current message
- Capability refusal is keyword+intent (double defence — catches misclassified messages too)
</decisions_made>

<blockers>

None blocking. All bugs resolved. Deferred items are non-blocking for VPS deployment.
</blockers>

<context>
This was a full milestone execution session — all 3 phases planned, executed, and
verified in one pass using MY MODE --execute-only (plans were already in place).

The bot works. Tasks, notes, continuity signal all confirmed live on Windows dev.
Ollama was offline during testing so LLM-dependent features (history recall,
retrieval, summarization content) couldn't be confirmed — but all structural wiring
verified via 33 tests and code inspection.

The five deferred items are documented with exact steps and expected outputs in
.planning/phases/03-context-budget-manager/03-VERIFICATION.md.
</context>

<next_action>
1. Connect to VPS (tomorrow 2026-03-30)
2. Pull latest: git pull origin master
3. Install deps: pip install -r requirements.txt
4. Install job queue: pip install "python-telegram-bot[job-queue]"
5. Copy .env to VPS (not committed — contains TELEGRAM_TOKEN, TELEGRAM_USER_ID, DB_PATH)
6. Start Ollama: ollama run qwen3:4b
7. Start bot: python -m app.main
8. Run 5 deferred checks from 03-VERIFICATION.md
9. Reply "approved" to close Phase 3 verification
10. Then: /gsd:complete-milestone v1.0
</next_action>
